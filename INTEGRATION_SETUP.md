# 🔧 Інструкція з налаштування інтеграцій

## ✅ ВИПРАВЛЕНО
- **AssertionError**: Видалено дубльовані маршрути Flask (`/force_run` та `index()`)
- Код тепер запускається без помилок

---

## 📋 Trello Integration

### Крок 1: Отримання API Key та Token

1. Перейдіть на https://trello.com/app-key
2. Скопіюйте **API Key**
3. Натисніть на кнопку **Token** (зелена кнопка на тій самій сторінці)
4. Дозвольте доступ і скопіюйте **Token**

### Крок 2: Отримання Board ID

1. Відкрийте вашу дошку Trello в браузері
2. Подивіться на URL: `https://trello.com/b/BOARD_ID/board-name`
3. Скопіюйте `BOARD_ID` з URL

### Крок 3: Додайте в .env

```env
TRELLO_API_KEY=ваш_api_key
TRELLO_TOKEN=ваш_token
TRELLO_BOARD_ID=ваш_board_id
TRELLO_LIST_NAME=Важливі завдання
```

### Як це працює:
- Коли впевненість AI >= 80%, автоматично створюється картка в Trello
- Картка містить назву чату, звіт і відсоток впевненості

---

## 📅 Google Calendar Integration

### Крок 1: Увімкнення Google Calendar API

1. Перейдіть на https://console.cloud.google.com/
2. Створіть новий проект (або виберіть існуючий)
3. Увімкніть **Google Calendar API**:
   - Натисніть "Enable APIs and Services"
   - Знайдіть "Google Calendar API"
   - Натисніть "Enable"

### Крок 2: Створення OAuth 2.0 Credentials

1. В Google Cloud Console йдіть в **APIs & Services** → **Credentials**
2. Натисніть **Create Credentials** → **OAuth client ID**
3. Виберіть **Application type**: Desktop app
4. Натисніть **Create**
5. Завантажте файл JSON (це ваш `credentials.json`)

### Крок 3: Налаштування

1. Покладіть файл `credentials.json` в корінь проекту (D:\projects\AIBI_Project\)
2. У `.env` встановіть:
   ```env
   ENABLE_GOOGLE_CALENDAR=true
   ```

### Крок 4: Перша авторизація

При першому запуску:
1. Відкриється браузер з запитом на авторизацію
2. Виберіть свій Google акаунт
3. Дозвольте доступ до календаря
4. Файл `token.json` буде створено автоматично

### Як це працює:
- Коли AI позначає звіт як `needs_review` (впевненість < 95%), створюється нагадування в календарі через 2 години
- Нагадування містить повний звіт AI

---

## 📦 Встановлення залежностей

Запустіть:
```bash
pip install -r requirements.txt
```

---

## 🚀 Запуск проекту

```bash
python main.py
```

Відкрийте браузер: http://localhost:8080

---

## 🔍 Тестування

1. Натисніть **"⚡ ПРИМУСОВА ПЕРЕВІРКА ЗАРАЗ"** на веб-інтерфейсі
2. Перевірте консоль на наявність повідомлень:
   - `📋 Створено картку в Trello: ...`
   - `📅 Створено нагадування в Calendar: ...`

---

## ⚙️ Налаштування

### Зміна частоти перевірки
В `main.py` (рядок 148):
```python
scheduler.add_job(func=scheduled_task, trigger="interval", minutes=20)
```

### Зміна порогу впевненості для Trello
В `main.py` (рядок 121):
```python
if trello and result['confidence'] >= 80:  # Змініть 80 на інше значення
```

### Зміна часу нагадування Calendar
В `main.py` (рядок 132):
```python
reminder_time = datetime.now() + timedelta(hours=2)  # Змініть hours=2
```

---

## 🐛 Усунення проблем

### Trello не створює картки
- Перевірте правильність API_KEY, TOKEN і BOARD_ID
- Переконайтеся, що список з назвою `TRELLO_LIST_NAME` існує на дошці

### Google Calendar не створює події
- Переконайтеся, що файл `credentials.json` в корені проекту
- Видаліть `token.json` і пройдіть авторизацію заново
- Перевірте, що `ENABLE_GOOGLE_CALENDAR=true`

### AssertionError все ще з'являється
- Переконайтеся, що ви збережили оновлений `main.py`
- Перезапустіть Flask сервер
